---
phase: 260606-p2k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - InterfazHubSpot.Tests.Unit/Integration/IntegracionColaIdentificadorTests.cs
  - InterfazHubSpot.Tests.Unit/Diagnostics/NullProcesoPasoReporterTests.cs
  - InterfazHubSpot.Tests.Unit/Diagnostics/ErpConnectivityProbeTests.cs
  - InterfazHubSpot.Tests.Unit/HubSpot/HubSpotCrmClientEndpointsTests.cs
  - InterfazHubSpot.Tests.Unit/HubSpot/HubSpotIntegracionRunnerPayloadTests.cs
  - InterfazHubSpot.IntegrationTests/Managers/ProcesosSpertaHubSpotManagerLiveTests.cs
  - InterfazHubSpot.IntegrationTests/Managers/ClienteIntegracionManagerLiveTests.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "El runner principal (HubSpotIntegracionRunner) tiene tests que verifican el mapeo de payload company/contact y la rama parseo-inválido"
    - "IntegracionColaIdentificador cubre todas las ramas: item nulo, TipoEntidad inválido, Identificador<=0, caso OK"
    - "HubSpotCrmClient cubre los endpoints aún no probados: search company, search contact, upsert company (POST y PATCH), upsert contact, associate, batch update"
    - "ErpConnectivityProbe.TryParseConnectionPieces y SanitizeSqlError quedan probados sin necesidad de SQL real"
    - "NullProcesoPasoReporter queda cubierto (smoke)"
    - "Los managers ligados a EF6 (ProcesosSpertaHubSpotManager, ClienteIntegracionManager) tienen tests de integración tagged Category=Live que documentan el contrato contra MSGestion (excluidos del run default)"
    - "La suite unitaria completa corre verde con Test-InterfazHubSpot.ps1"
  artifacts:
    - path: "InterfazHubSpot.Tests.Unit/Integration/IntegracionColaIdentificadorTests.cs"
      provides: "Cobertura de las 4 ramas de TryGetClienteId"
      contains: "IntegracionColaIdentificador.TryGetClienteId"
    - path: "InterfazHubSpot.Tests.Unit/Diagnostics/ErpConnectivityProbeTests.cs"
      provides: "Tests de parseo de connection string y sanitizado de error (sin tocar SQL)"
      contains: "ErpConnectivityProbe"
    - path: "InterfazHubSpot.Tests.Unit/HubSpot/HubSpotCrmClientEndpointsTests.cs"
      provides: "Tests de los endpoints CRM v3 con HttpMessageHandler fake"
      contains: "HubSpotCrmClient"
    - path: "InterfazHubSpot.Tests.Unit/HubSpot/HubSpotIntegracionRunnerPayloadTests.cs"
      provides: "Tests del mapeo company/contact properties vía reflexión sobre métodos privados"
      contains: "HubSpotIntegracionRunner"
    - path: "InterfazHubSpot.IntegrationTests/Managers/ProcesosSpertaHubSpotManagerLiveTests.cs"
      provides: "Contrato del manager de cola contra MSGestion real (Category=Live)"
      contains: "Trait(\"Category\", \"Live\")"
    - path: "InterfazHubSpot.IntegrationTests/Managers/ClienteIntegracionManagerLiveTests.cs"
      provides: "Contrato SP USP_Integracion_HubSpot_Cliente_Obtener y _CuentaCorriente_Pagina (Category=Live)"
      contains: "Trait(\"Category\", \"Live\")"
  key_links:
    - from: "InterfazHubSpot.Tests.Unit/HubSpot/HubSpotCrmClientEndpointsTests.cs"
      to: "InterfazHubSpot.Business/HubSpot/HubSpotCrmClient.cs"
      via: "HubSpotCrmClient(HubSpotConfiguration, HttpClient) — constructor interno con HttpClient inyectable"
      pattern: "new HubSpotCrmClient\\("
    - from: "InterfazHubSpot.Tests.Unit/HubSpot/HubSpotIntegracionRunnerPayloadTests.cs"
      to: "InterfazHubSpot.Business/HubSpot/HubSpotIntegracionRunner.cs"
      via: "Reflexión sobre BuildCompanyProperties / BuildContactProperties (privados)"
      pattern: "BindingFlags\\.NonPublic"
---

<objective>
Cerrar los gaps de cobertura de tests sobre las clases de negocio no triviales que hoy quedan sin ejercitar: `HubSpotIntegracionRunner` (mapeo de payload + rama de error), `HubSpotCrmClient` (endpoints faltantes), `IntegracionColaIdentificador`, `ErpConnectivityProbe` (parseo/sanitizado sin SQL) y `NullProcesoPasoReporter`. Los managers EF6 (`ProcesosSpertaHubSpotManager`, `ClienteIntegracionManager`) se cubren con tests de integración tageados `Category=Live` para que documenten el contrato contra MSGestion sin romper el run por defecto.

Purpose: Llegar a una cobertura efectiva alta (~90 % de las clases Business no triviales: HubSpot/, Integration/, Diagnostics/) sin requerir DB ni token reales en el run por defecto, y dejar el camino marcado para los tests Live cuando haya ambiente.

Output: 5 archivos de tests unitarios nuevos en `InterfazHubSpot.Tests.Unit/`, 2 archivos de tests Live nuevos en `InterfazHubSpot.IntegrationTests/`. Compilan y `Test-InterfazHubSpot.ps1` pasa (los Live quedan excluidos por filtro).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@InterfazHubSpot.Tests.Unit/InterfazHubSpot.Tests.Unit.csproj
@InterfazHubSpot.IntegrationTests/InterfazHubSpot.IntegrationTests.csproj
@InterfazHubSpot.Tests.Unit/HubSpotInternalsTests.cs
@InterfazHubSpot.Tests.Unit/DevelopmentHubSpotStubTests.cs
@InterfazHubSpot.Business/HubSpot/HubSpotIntegracionRunner.cs
@InterfazHubSpot.Business/HubSpot/HubSpotCrmClient.cs
@InterfazHubSpot.Business/Integration/IntegracionColaIdentificador.cs
@InterfazHubSpot.Business/Diagnostics/ErpConnectivityProbe.cs
@InterfazHubSpot.Business/Diagnostics/NullProcesoPasoReporter.cs
@InterfazHubSpot.Business/Managers/ProcesosSpertaHubSpotManager.cs
@InterfazHubSpot.Business/Managers/ClienteIntegracionManager.cs

<interfaces>
<!-- Contratos clave para escribir los tests sin explorar el código -->

InterfazHubSpot.Business/HubSpot/HubSpotCrmClient.cs:
  - internal sealed class HubSpotConfiguration  // ctor lee ConfigurationManager — para tests usar reflexión o sobreescribir AppSettings
    Members: BaseUrl, PrivateAppToken, PropertyMastersoftId, PropertyManejoCuentaCorriente,
             DelayMsBetweenCalls, CuentaCorrientePageSize, UseDevelopmentMock, TienePrivateAppToken, ValidarToken()
  - internal sealed class HubSpotCrmClient
    Ctor: HubSpotCrmClient(HubSpotConfiguration cfg, HttpClient httpClient)  // httpClient inyectable
    Methods (internal async):
      - Task<string> SearchCompanyIdByMastersoftIdAsync(string mastersoftId)
      - Task<string> UpsertCompanyAsync(string idOrNull, JObject properties)   // POST si null, PATCH si id
      - Task<string> SearchContactIdByEmailAsync(string email)                // null si email vacío sin HTTP call
      - Task<Tuple<bool,string>> UpsertContactAsync(string idOrNull, JObject properties)
      - Task AssociateContactToCompanyAsync(string contactId, string companyId)  // PUT, lanza si !success
      - Task BatchUpdateCompaniesManejoCcAsync(IReadOnlyList<Tuple<string,string>> hubIdAndTexto)

InterfazHubSpot.Business/Integration/IntegracionColaIdentificador.cs:
  - public static bool TryGetClienteId(ProcesosSpertaHubSpot item, out int clienteId, out string errorMessage)
    Ramas: item==null, TipoEntidad != "Cliente" (case-insensitive), Identificador<=0, OK
    Constante: IntegracionTipoEntidad.Cliente

InterfazHubSpot.Business/HubSpot/HubSpotIntegracionRunner.cs (métodos privados a probar vía reflexión):
  - private JObject BuildCompanyProperties(ClienteDatosDto ms, int clientePk, string codigoCliente)
    Mapea: name (Coalesce RazonSocial/ApellidoYNombre/Contacto), nombre_fantasia, cuitcuil, nro_cliente,
           mastersoft_id_, adress, puerta, city, zip, state, Country, zona_vta, vendedor,
           responsable_de_cuenta, lista_de_precios, condicion_de_venta, dias_para_deuda,
           limite_de_credito, categoria_cliente, direccion_{1..3}_{domicilio,cp,localidad,provincia}
  - private static JObject BuildContactProperties(ContactoDto)  → firstname, email, phone, sector
  - private static string Coalesce(params string[]) → primer no-vacío trimmed o null

InterfazHubSpot.Business/Diagnostics/ErpConnectivityProbe.cs (privados a probar vía reflexión):
  - private static void TryParseConnectionPieces(string cs, out string ds, out string ic, out string uid)
    Casos: cs vacío → todo "", cs con Data Source/Initial Catalog/User ID, cs con Integrated Security
  - private static string SanitizeSqlError(string message)  // trunca a 2000 + "..."

InterfazHubSpot.Business/Diagnostics/NullProcesoPasoReporter.cs:
  - public static readonly IProcesoPasoReporter Instance
  - RegistrarPaso(...) // no-op

Tests existentes para no duplicar:
  - HubSpotInternalsTests.cs: 429 retry, 401 stop, throttle
  - DevelopmentHubSpotStubTests.cs: stub handler
  - DiagnosticsTextHelperTests.cs, IntegracionColaEstadosTests.cs, ProcesoPasoReporterTests.cs, ClienteIntegracionMapperTests.cs

Patrón de fake handler (ver HubSpotInternalsTests.cs):
```csharp
private sealed class CapturingHandler : HttpMessageHandler
{
    public List<HttpRequestMessage> Requests { get; } = new List<HttpRequestMessage>();
    public Queue<HttpResponseMessage> Responses { get; } = new Queue<HttpResponseMessage>();
    protected override Task<HttpResponseMessage> SendAsync(HttpRequestMessage req, CancellationToken ct)
    { Requests.Add(req); return Task.FromResult(Responses.Dequeue()); }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests unitarios puros (Identificador + NullReporter + ErpConnectivityProbe parseo)</name>
  <files>
    InterfazHubSpot.Tests.Unit/Integration/IntegracionColaIdentificadorTests.cs,
    InterfazHubSpot.Tests.Unit/Diagnostics/NullProcesoPasoReporterTests.cs,
    InterfazHubSpot.Tests.Unit/Diagnostics/ErpConnectivityProbeTests.cs
  </files>
  <behavior>
    IntegracionColaIdentificadorTests:
      - TryGetClienteId_ItemNulo_DevuelveFalseConMensaje("Ítem de cola nulo.")
      - TryGetClienteId_TipoEntidadVacio_DevuelveFalseConMensajeMencionando("Cliente")
      - TryGetClienteId_TipoEntidadOtroValor_DevuelveFalseConMensajeMencionando(tipoRecibido)
      - TryGetClienteId_TipoEntidadClienteCaseInsensitive_AceptaVariantes("cliente","CLIENTE","Cliente")
      - TryGetClienteId_IdentificadorCero_DevuelveFalseConMensajeMayorQueCero
      - TryGetClienteId_IdentificadorNegativo_DevuelveFalseConMensajeMayorQueCero
      - TryGetClienteId_Ok_DevuelveTrueYClienteIdIgualAIdentificador(positivo)
    NullProcesoPasoReporterTests:
      - Instance_NoEsNull
      - Instance_EsSingleton (dos llamadas a .Instance referencia igual)
      - RegistrarPaso_ConCualquierArgumento_NoLanza (incluye null en mensaje/datos)
    ErpConnectivityProbeTests (sólo métodos privados invocados via reflexión; cero SQL):
      - TryParseConnectionPieces_StringVacio_DevuelveCadenasVacias
      - TryParseConnectionPieces_FormatoEstandar_ExtraeDataSourceCatalogoUsuario("Data Source=srv;Initial Catalog=db;User ID=u;Password=p")
      - TryParseConnectionPieces_IntegratedSecurity_DevuelveUsuarioIntegratedSecurity
      - TryParseConnectionPieces_AliasServerDatabase_ExtraeCorrectamente("Server=s;Database=d;Uid=u;Pwd=p")
      - TryParseConnectionPieces_StringInvalido_NoLanza (fallback ExtractKeyFallback)
      - SanitizeSqlError_Null_DevuelveStringEmpty
      - SanitizeSqlError_Corto_DevuelveTalCual
      - SanitizeSqlError_MasDeDosMil_Trunca_Y_SufijaElipsis (verificar Length==2003 y termina en "...")
  </behavior>
  <action>
    Crear los tres archivos de tests unitarios. Usar xUnit 2.4.1 (ya en csproj — no agregar packages). Patrón AAA simple, sin Moq (no es necesario aquí).

    Para IntegracionColaIdentificadorTests: instanciar `ProcesosSpertaHubSpot` (entidad EF6 — POCO, debería ser instanciable con `new`) y poblar `TipoEntidad`/`Identificador`. Usar la constante `IntegracionTipoEntidad.Cliente` (no hardcodear "Cliente").

    Para NullProcesoPasoReporterTests: acceder a `NullProcesoPasoReporter.Instance` y verificar identidad referencial entre dos accesos; llamar `RegistrarPaso` con todos null y con valores normales y verificar que no lanza.

    Para ErpConnectivityProbeTests: invocar los métodos privados estáticos `TryParseConnectionPieces` y `SanitizeSqlError` vía `typeof(ErpConnectivityProbe).GetMethod(name, BindingFlags.Static | BindingFlags.NonPublic)`. NO invocar `ProbarMsgestion` aquí (requiere DB). Para `TryParseConnectionPieces`, las firmas con `out` se llaman con `object[]{cs, null, null, null}` y se leen los args devueltos por `Invoke`.

    NO crear carpetas físicas si los archivos `.cs` con namespace correcto bastan; el SDK-style csproj incluye recursivamente. Namespaces: `InterfazHubSpot.Tests.Unit.Integration`, `InterfazHubSpot.Tests.Unit.Diagnostics`.
  </action>
  <verify>
    <automated>pwsh -NoProfile -File InterfazHubSpot/Scripts/agent/Test-InterfazHubSpot.ps1 -Filter "FullyQualifiedName~IntegracionColaIdentificadorTests|FullyQualifiedName~NullProcesoPasoReporterTests|FullyQualifiedName~ErpConnectivityProbeTests"</automated>
  </verify>
  <done>
    Los tres archivos compilan, los tres conjuntos de tests pasan (≥7+3+8=18 tests), no se introdujo ninguna referencia a SpertaApi/MSFwk/SpertaFwk en el código nuevo (verify legacy-grep sigue limpio).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests HubSpotCrmClient (endpoints faltantes) + HubSpotIntegracionRunner (payload mapping vía reflexión)</name>
  <files>
    InterfazHubSpot.Tests.Unit/HubSpot/HubSpotCrmClientEndpointsTests.cs,
    InterfazHubSpot.Tests.Unit/HubSpot/HubSpotIntegracionRunnerPayloadTests.cs
  </files>
  <behavior>
    HubSpotCrmClientEndpointsTests (todos con `CapturingHandler` igual al patrón de HubSpotInternalsTests):
      - SearchCompanyIdByMastersoftIdAsync_TotalCero_DevuelveNull (respuesta {"total":0,"results":[]})
      - SearchCompanyIdByMastersoftIdAsync_ConResultado_DevuelveIdDelPrimerResultado y verifica POST a "/crm/v3/objects/companies/search" con header Bearer y body que contiene `mastersoft_id_` y el valor pasado
      - UpsertCompanyAsync_SinIdExistente_HacePOST_A_Companies (verifica método=POST, URL termina en `/companies`)
      - UpsertCompanyAsync_ConIdExistente_HacePATCH_A_CompaniesSlashId (verifica método=PATCH, URL contiene el id escape-encoded)
      - SearchContactIdByEmailAsync_EmailVacio_DevuelveNull_SinHttp (handler.Requests.Count==0)
      - SearchContactIdByEmailAsync_TotalCero_DevuelveNull
      - SearchContactIdByEmailAsync_TrimmeaElEmail (body debe contener email trimmed)
      - UpsertContactAsync_SinId_DevuelveTupleCreatedTrueYIdDeRespuesta_HacePOST
      - UpsertContactAsync_ConId_DevuelveTupleCreatedFalseYIdOriginal_HacePATCH
      - AssociateContactToCompanyAsync_HttpOk_NoLanza_Y_UsaPUT
      - AssociateContactToCompanyAsync_Http500_LanzaHttpRequestException_ConMensajeIncluyeStatusCode
      - BatchUpdateCompaniesManejoCcAsync_ListaVacia_NoHaceHttp
      - BatchUpdateCompaniesManejoCcAsync_LlamaBatchUpdate_ConTodasLasTuplas (body parsea a JObject con `inputs` array length==N, cada item con `id` y `properties.manejo_cuenta_corriente`)
    HubSpotIntegracionRunnerPayloadTests (todos vía reflexión sobre métodos privados — el runner se instancia con `null` MSContext sólo si los métodos invocados son static; para BuildCompanyProperties que NO es static, instanciar usando `FormatterServices.GetUninitializedObject(typeof(HubSpotIntegracionRunner))` y setear `_hubCfg` vía reflexión a una `HubSpotConfiguration` por defecto — ver nota abajo):
      - Coalesce_TodosNullOEspacios_DevuelveNull (static, fácil)
      - Coalesce_PrimerNoVacio_DevuelveTrimmed
      - BuildContactProperties_Completa_MapeaTodosCampos (static): firstname/email/phone/sector según ContactoDto
      - BuildContactProperties_CamposNull_DevuelveCadenasVaciasNoNull
      - BuildCompanyProperties_RazonSocialPresente_UsaRazonSocialComoName
      - BuildCompanyProperties_SinRazonSocial_UsaApellidoYNombreComoName
      - BuildCompanyProperties_SinRazonNiApellido_UsaContactoComoName
      - BuildCompanyProperties_MapeaMastersoftIdConElNombreDePropiedadConfigurado (default "mastersoft_id_")
      - BuildCompanyProperties_DireccionesEntregaMasDeTres_SoloMapeaTres (direccion_1, direccion_2, direccion_3, NO direccion_4)
      - BuildCompanyProperties_SinDirecciones_NoLanza
      - BuildCompanyProperties_CamposNullEnDto_DevuelveCadenasVaciasNoNull
  </behavior>
  <action>
    HubSpotCrmClientEndpointsTests: replicar el patrón de `HubSpotInternalsTests.cs` (HttpMessageHandler que captura `Requests` y responde de `Queue<HttpResponseMessage>`). Instanciar `HubSpotConfiguration` directamente (es internal, accesible por InternalsVisibleTo) — su ctor lee `ConfigurationManager.AppSettings`; para tests con token, settear `ConfigurationManager.AppSettings["HubSpot:PrivateAppToken"] = "tok"` antes de instanciar, o ajustar `HubSpot:DelayMillisecondsBetweenCalls = "0"` para que los tests no esperen 120 ms (`ConfigurationManager.AppSettings.Set("HubSpot:DelayMillisecondsBetweenCalls", "0")`). Construir el cliente vía `new HubSpotCrmClient(cfg, new HttpClient(handler) { BaseAddress = null })`.

    Para verificar URLs y bodies: capturar la `HttpRequestMessage` y leer `request.RequestUri.AbsolutePath`, `request.Method`, `request.Headers.Authorization?.Scheme/Parameter`, y `await request.Content.ReadAsStringAsync()` parseado con `JObject.Parse`.

    HubSpotIntegracionRunnerPayloadTests: para los métodos privados `static` (Coalesce, BuildContactProperties), invocar directamente con `typeof(HubSpotIntegracionRunner).GetMethod("Coalesce", BindingFlags.NonPublic | BindingFlags.Static).Invoke(null, new object[]{ new[]{...} })`. Para `BuildCompanyProperties` que es de instancia y usa `_hubCfg.PropertyMastersoftId`: crear instancia sin ctor con `System.Runtime.Serialization.FormatterServices.GetUninitializedObject(typeof(HubSpotIntegracionRunner))`, settear vía reflexión el field `_hubCfg` con una `HubSpotConfiguration` nueva (que devuelve "mastersoft_id_" por default), invocar el método.

    Construir DTOs `ClienteDatosDto`, `ContactoDto`, `DireccionEntregaDto` con los campos requeridos. Verificar propiedades del `JObject` resultado con `((string)props["name"])` y `props.Property("direccion_4_domicilio") == null` etc.

    Si `ConfigurationManager.AppSettings.Set` no persiste entre tests, agrupar tests que comparten config en una clase con `IClassFixture` o usar `AppSettings.Set` en cada test (es proceso-global pero estos archivos sólo corren bajo `Test-InterfazHubSpot.ps1`).

    NO agregar paquetes NuGet nuevos (xUnit ya disponible, Moq no necesario para handler simple).
  </action>
  <verify>
    <automated>pwsh -NoProfile -File InterfazHubSpot/Scripts/agent/Test-InterfazHubSpot.ps1 -Filter "FullyQualifiedName~HubSpotCrmClientEndpointsTests|FullyQualifiedName~HubSpotIntegracionRunnerPayloadTests"</automated>
  </verify>
  <done>
    Ambos archivos compilan; ≥13 tests de endpoints + ≥11 tests de payload mapping pasan; los tests de payload validan explícitamente que `mastersoft_id_` aparece en el JObject company y que `direccion_4_*` NO aparece nunca.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests de integración Live para Managers EF6 + verify final</name>
  <files>
    InterfazHubSpot.IntegrationTests/Managers/ProcesosSpertaHubSpotManagerLiveTests.cs,
    InterfazHubSpot.IntegrationTests/Managers/ClienteIntegracionManagerLiveTests.cs
  </files>
  <action>
    Crear dos archivos de tests de INTEGRACIÓN tageados `[Trait("Category","Live")]` a NIVEL DE CADA TEST (no de clase, salvo que todos los tests de la clase sean Live). Estos tests requieren conexión real a MSGestion y por eso quedan EXCLUIDOS del run default por `Test-InterfazHubSpot.ps1` (que filtra `Category!=Live`). Su valor es documentar el contrato cuando haya ambiente.

    ProcesosSpertaHubSpotManagerLiveTests:
      - [Fact, Trait("Category","Live")] ContarPendientes_DestinoHubSpot_DevuelveNumeroNoNegativo
      - [Fact, Trait("Category","Live")] ContarEnProceso_DestinoHubSpot_DevuelveNumeroNoNegativo
      - [Fact, Trait("Category","Live")] ListarMuestraPendientes_MaxItemsCero_DevuelveListaVacia
      - [Fact, Trait("Category","Live")] ReclamarPendientes_Y_MarcarOk_RoundTrip (inserta fila Pendiente vía SQL crudo en setup, reclama, marca Ok, verifica estado final == "Ok") — si no es seguro insertar, marcar [Fact(Skip="requires writable test DB")]
      - [Fact, Trait("Category","Live")] MarcarError_TruncaMensajeMayorA8000Caracteres (verifica longitud final ≤ 8000)
      - [Fact, Trait("Category","Live")] ReponerEnCola_VuelveAEstadoPendiente_LimpiaFechas

    ClienteIntegracionManagerLiveTests:
      - [Fact, Trait("Category","Live")] ObtenerClienteParaHubSpot_ClienteExistente_DevuelveDtoNoNulo_ConClienteId (cliente id parametrizable vía env `INTEGRATION_TEST_CLIENTE_ID`; si no está set → Skip)
      - [Fact, Trait("Category","Live")] ObtenerClienteParaHubSpot_ClienteInexistente_DevuelveDtoConClienteIdCero_O_Null (documenta el contrato actual del mapper)
      - [Fact, Trait("Category","Live")] ObtenerPaginaCuentaCorriente_PageSize10_DevuelvePaginaConHayMasBoolean_Y_ItemsConteoMenorOIgualA10

    Para construir `MSContext`: revisar cómo se instancia en el resto del repo (probablemente `new MSContext()` que toma la connection string `MSGestion` de `App.config`). Los `App.config` de los proyectos de test ya hacen `CopyToOutputDirectory=PreserveNewest`. Si `MSContext` requiere parámetros, inspeccionar `InterfazHubSpot.Mapping.Context.MSGestionContext` y el resto del código que ya lo instancia (`HubSpotIntegracionRunner` ctor).

    Si la API exacta de `MSContext` no permite instanciación sin parámetros adicionales, marcar los tests con `[Fact(Skip="MSContext requiere wiring DI; ver issue")]` PERO dejar el archivo creado para que cuando se complete el wiring se eliminen los Skip.

    Verify legacy-grep: confirmar que ningún archivo nuevo introduce las strings prohibidas (`SpertaApi`, `MSFwk`, `SpertaFwk`).
  </action>
  <verify>
    <automated>pwsh -NoProfile -File InterfazHubSpot/Scripts/agent/Verify-InterfazHubSpot.ps1</automated>
  </verify>
  <done>
    Ambos archivos compilan; ningún test Live se ejecuta en el run por defecto (todos filtrados por Category=Live); `Verify-InterfazHubSpot.ps1` completo pasa (build + tests no-Live + legacy-grep limpio).
  </done>
</task>

</tasks>

<verification>
- `pwsh -NoProfile -File InterfazHubSpot/Scripts/agent/Test-InterfazHubSpot.ps1` corre verde con ≥40 tests adicionales pasando (suma de Tasks 1 + 2; Task 3 todos excluidos)
- `pwsh -NoProfile -File InterfazHubSpot/Scripts/agent/Verify-InterfazHubSpot.ps1` completo pasa (build + tests + legacy-grep)
- No se agregaron paquetes NuGet nuevos a los .csproj (xUnit 2.4.1 + Test.Sdk 16.11.0 ya cubren todo)
- Ningún archivo nuevo contiene `SpertaApi`, `MSFwk`, `SpertaFwk`
- Coverage manual (inspección visual): `HubSpotCrmClient` cubre search-company, search-contact, upsert-company POST+PATCH, upsert-contact POST+PATCH, associate, batch-update (más 429/401/throttle preexistentes) ⇒ 100% endpoints. `HubSpotIntegracionRunner` cubre BuildCompanyProperties/BuildContactProperties/Coalesce. `IntegracionColaIdentificador` cubre todas las ramas. `ErpConnectivityProbe` cubre parseo+sanitizado (probar no se cubre sin Live — aceptado). `NullProcesoPasoReporter` cubierto trivialmente.
</verification>

<success_criteria>
- Cobertura efectiva de las clases Business no triviales (HubSpot/, Integration/, Diagnostics/, Managers/) llega a ~90% por inspección de ramas
- Run por defecto (`Test-InterfazHubSpot.ps1`) verde, rápido (sin DB), sin tokens reales
- Los tests Live tageados existen y documentan el contrato esperado contra MSGestion para cuando haya ambiente
- Tests existentes (HubSpotInternalsTests, DevelopmentHubSpotStubTests, etc.) siguen pasando — no se modificó ninguno
</success_criteria>

<output>
After completion, create `.planning/quick/260606-p2k-quiero-que-el-sistema-tenga-test-unitari/260606-p2k-01-SUMMARY.md`
</output>
